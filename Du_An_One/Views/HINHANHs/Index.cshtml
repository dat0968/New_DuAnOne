@model IEnumerable<Du_An_One.Models.HINHANH>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HinhAnh)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaSP)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HinhAnh)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaSP)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaHinhAnh">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaHinhAnh">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaHinhAnh">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
